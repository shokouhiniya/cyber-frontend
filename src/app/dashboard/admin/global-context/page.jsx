import { AdminGlobalContextView } from 'src/sections/admin/global-context/view';

export const metadata = { title: 'متغیرهای عمومی | مدیریت' };

export default function Page() {
  return <AdminGlobalContextView />;
}
